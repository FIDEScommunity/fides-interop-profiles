{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fides.community/schemas/vocabulary/v1",
  "title": "Vocabulary",
  "description": "Glossary of terms for catalog filters and interop matrix; used for [i] info popups.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "terms"],
  "properties": {
    "version": { "type": "string" },
    "terms": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "description": { "type": "string" },
          "title": {
            "type": "string",
            "description": "Optional human-readable label for glossary display; defaults to a formatted version of the term key."
          },
          "url": { "type": "string", "format": "uri" },
          "aliases": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Synonyms / alternative spellings that should resolve to this term (case-insensitive)."
          }
        }
      }
    }
  }
}
